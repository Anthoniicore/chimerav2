name: Build Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Run build.bat
      shell: cmd
      run: |
        cd path/to/your/build/script
        build.bat
